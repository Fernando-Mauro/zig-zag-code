#include <bits/stdc++.h>

using namespace std;

int main(){
    // 7 > 6 -> verdadero
    // 7 != 7 ->  falso
    // 10 > 10 -> falso
    // 10 >= 10 -> verdadero
    // 10 == 10 -> verdadero
    int primero;
    cin >> primero;
int segundo;
cin >> segundo;
    // 1 lunes
    // 2 es martes
    // true y true= true
    // false or true = true
    if(primero > 18 || segundo > 18 && maslkdmsakl && ||){

    }

    return 0;
}
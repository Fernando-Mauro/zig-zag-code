#include <bits/stdc++.h> 

using namespace std;

typedef vector <vector <int>> matriz;

int main(){
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    int n, m;
    cin >> n >> m;
    matriz papalote(n, vector <int> (m));
    for(int i = 0; i < n; ++i){
        for(int j = 0; j < m; ++j){
            
        }
    }

    return 0;
}
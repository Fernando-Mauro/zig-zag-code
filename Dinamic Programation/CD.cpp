#include <bits/stdc++.h>

using namespace std;

int main(){
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    int objetivo;
    while(cin >> objetivo){
        int n;
        cin >> n;
        vector <int> numbers(n);
        for(auto &it : numbers){
            cin >> it;  
        }
    }
    return 0;
}
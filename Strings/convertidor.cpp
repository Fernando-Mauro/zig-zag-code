#include <bits/stdc++.h> 

using namespace std;

int main(){
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    int type;
    cin >> type;
    switch (type){
    case /* constant-expression */:
        /* code */
        break;
    
    default:
        break;
    }
    return 0;
}   